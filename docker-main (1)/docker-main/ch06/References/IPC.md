

# 🐧 리눅스 IPC

리눅스 시스템에서 프로세스들은 각각 **독립된 메모리 공간**을 갖고 있습니다. 그렇다면 이들 간에 데이터를 주고받으려면 어떻게 해야 할까요? 답은 바로 **IPC(Inter-Process Communication)** 입니다.


---

## 🧭 IPC란?

**IPC(Inter-Process Communication)** 는 말 그대로 **프로세스 간 통신**을 의미합니다.

* 🏠 각 프로세스는 **독립된 주소 공간**을 갖고 있어 직접 접근 불가
* 🔄 데이터를 주고받기 위해선 **커널을 통한 매개체** 필요
* 📡 IPC는 단순 데이터 교환을 넘어 **이벤트 알림, 동기화, 메시징**까지 지원

---

## 🗂️ 리눅스 IPC 분류 한눈에 보기

| 유형              | 데이터 교환 방식 | 속도 🚀  | 복잡도 🧩 | 사용 사례      |
| --------------- | --------- | ------ | ------ | ---------- |
| 파이프(Pipes)      | 바이트 스트림   | 중간     | 낮음     | 부모-자식 프로세스 |
| 메시지 큐(Message)  | 구조화 메시지   | 중간     | 중간     | 이벤트 큐, 알림  |
| 공유 메모리(Shared)  | 메모리 직접 접근 | **빠름** | 높음     | 고성능 데이터 교환 |
| 시그널(Signals)    | 알림/이벤트    | 낮음     | 낮음     | 이벤트 트리거    |
| 소켓(Sockets)     | 스트림/데이터그램 | 중간     | 중간     | 로컬/네트워크 통신 |
| 세마포어(Semaphore) | 동기화 전용    | N/A    | 중간     | 락, 동기화 제어  |

---

## 🔍 주요 IPC 메커니즘 심층 분석

### 1️⃣ 파이프 & 네임드 파이프(FIFO) 🥤

* **익명 파이프**: `|` 로 흔히 쓰이는 바로 그것
* **네임드 파이프(FIFO)**: 파일 시스템 경로를 통해 독립 프로세스 간 통신 가능

```bash
mkfifo myfifo
echo "Hello IPC" > myfifo   # 쓰기
cat < myfifo                # 읽기
```

**장점**: 단순, 구현 쉬움
**단점**: 단방향, 대규모 통신 부적합

---

### 2️⃣ 메시지 큐(Message Queue) 📬

* 커널 내부 큐에 메시지를 넣고 꺼내는 방식
* POSIX(`mq_*`)와 System V(`msg*`) API 두 계열 존재

```c
msgsnd(msqid, &msg, sizeof(msg), 0);
msgrcv(msqid, &msg, sizeof(msg), 0, 0);
```

**장점**: 구조화된 메시지, 우선순위 가능
**단점**: 커널 복사 비용 존재, 크기 제한

---

### 3️⃣ 공유 메모리(Shared Memory) ⚡

* 데이터 복사 없이 **Zero-Copy** 실현
* 속도는 최고지만, **동기화 필수** → 세마포어와 함께 사용

```c
int shmid = shmget(key, size, IPC_CREAT | 0666);
char* data = (char*)shmat(shmid, NULL, 0);
```

**장점**: 최고의 성능
**단점**: 세마포어 없이 쓰면 Race Condition 위험

---

### 4️⃣ 시그널(Signals) 📢

* 프로세스에 이벤트 알림 비동기 전달
* 예: `kill(pid, SIGUSR1)`

**장점**: 단순 이벤트 트리거
**단점**: 데이터 전송 불가

---

### 5️⃣ 소켓(Sockets) 🌐

* `AF_UNIX` → 로컬 통신
* `AF_INET` → 네트워크 통신도 가능

```c
int sock = socket(AF_UNIX, SOCK_STREAM, 0);
connect(sock, ...);
send(sock, data, ...);
```

**장점**: 범용성 최고, 네트워크까지 확장
**단점**: 다른 IPC보다 상대적으로 느림

---

### 6️⃣ 세마포어(Semaphores) & 뮤텍스(Mutex) 🔒

* 임계 구역 보호용 동기화 메커니즘

```c
sem_t sem;
sem_init(&sem, 0, 1);
sem_wait(&sem);  // Lock
sem_post(&sem);  // Unlock
```

---

## ⚡ 성능 & 보안 고려사항

| 문제 요인       | 영향 ⚠️       | 해결책 💡          |
| ----------- | ----------- | --------------- |
| 커널-유저 모드 전환 | 성능 저하       | 공유 메모리 활용       |
| 동기화 부재      | 데이터 경합 발생   | 세마포어, 뮤텍스 사용    |
| 보안 이슈       | 권한 없는 접근 위험 | IPC 객체 권한 설정 철저 |

---

## 🏗️ 실전 아키텍처: 하이브리드 IPC

고성능 서버 아키텍처 예시:

1. **공유 메모리** → 데이터 고속 전송
2. **세마포어** → 동기화
3. **시그널** → 이벤트 알림

→ 결과: 성능, 안정성, 실시간성 모두 확보 🚀

---

## 🎯 마무리

리눅스 IPC는 **단일 툴**로 끝나지 않습니다.
요구사항에 따라 **혼합 아키텍처**를 설계해야 성능과 안정성을 동시에 확보할 수 있습니다.

| 요구사항       | 추천 IPC 조합     |
| ---------- | ------------- |
| 고성능 데이터 전송 | 공유 메모리 + 세마포어 |
| 실시간 알림     | 시그널 + 메시지 큐   |
| 분산 시스템     | 소켓 + 메시지 큐    |


